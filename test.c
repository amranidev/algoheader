#include "algoheader.h"

debut

entier a,b;

ecrire("donner un NUM 1 : ");

lire("%i",de a);

ecrire("donner un NUM 2 : ");

lire("%i",de b);

b = b + a;

ecrire("resultat : %i",b);

ecrire("\npuissans resultat %f",puis(b,a));

ecrire("\nb * 2 = %i",b);

fin
